== Rails Admin 

Build an admin panel using the rails_admin gem.

In combination with the 'devise' gem for authentication, using the 'rails_gem' is by far the easiest and fastest way to build an admin panel.